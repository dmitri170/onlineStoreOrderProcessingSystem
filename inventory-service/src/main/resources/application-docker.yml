server:
  port: 8080

grpc:
  server:
    port: 9091

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/online_store
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    com.example: DEBUG