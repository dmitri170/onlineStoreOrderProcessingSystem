spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/onlineStoreOrderProcessingSystem
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true